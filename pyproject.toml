[project]
name = "library"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.6",
    "django-ninja>=1.6.2",
    "pyjwt>=2.13.0",
]

[tool.uv.workspace]
members = [
    "mi_api",
]
